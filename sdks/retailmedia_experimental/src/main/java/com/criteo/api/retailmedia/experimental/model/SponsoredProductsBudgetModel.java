/*
 * Criteo API
 * Criteo API - RetailMedia
 *
 * The version of the OpenAPI document: Experimental
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.criteo.api.retailmedia.experimental.model;

import java.util.Objects;
import java.util.Arrays;
import com.criteo.api.retailmedia.experimental.model.BudgetCappingModel;
import com.criteo.api.retailmedia.experimental.model.PacingModel;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.List;
import org.openapitools.jackson.nullable.JsonNullable;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Map.Entry;
import java.util.Set;

import com.criteo.api.retailmedia.experimental.JSON;

/**
 * Financial intent of a SponsoredProducts campaign.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen")
public class SponsoredProductsBudgetModel {
  public static final String SERIALIZED_NAME_AMOUNT = "amount";
  @SerializedName(SERIALIZED_NAME_AMOUNT)
  private Double amount;

  public static final String SERIALIZED_NAME_CAPPINGS = "cappings";
  @SerializedName(SERIALIZED_NAME_CAPPINGS)
  private List<BudgetCappingModel> cappings = null;

  public static final String SERIALIZED_NAME_PACING = "pacing";
  @SerializedName(SERIALIZED_NAME_PACING)
  private PacingModel pacing;

  public SponsoredProductsBudgetModel() {
  }

  public SponsoredProductsBudgetModel amount(Double amount) {
    
    this.amount = amount;
    return this;
  }

   /**
   * Get amount
   * @return amount
  **/
  @javax.annotation.Nullable

  public Double getAmount() {
    return amount;
  }


  public void setAmount(Double amount) {
    this.amount = amount;
  }


  public SponsoredProductsBudgetModel cappings(List<BudgetCappingModel> cappings) {
    
    this.cappings = cappings;
    return this;
  }

  public SponsoredProductsBudgetModel addCappingsItem(BudgetCappingModel cappingsItem) {
    if (this.cappings == null) {
      this.cappings = null;
    }
    this.cappings.add(cappingsItem);
    return this;
  }

   /**
   * Per-period ceilings. Only capped periods are reported.
   * @return cappings
  **/
  @javax.annotation.Nullable

  public List<BudgetCappingModel> getCappings() {
    return cappings;
  }


  public void setCappings(List<BudgetCappingModel> cappings) {
    this.cappings = cappings;
  }


  public SponsoredProductsBudgetModel pacing(PacingModel pacing) {
    
    this.pacing = pacing;
    return this;
  }

   /**
   * Get pacing
   * @return pacing
  **/
  @javax.annotation.Nullable

  public PacingModel getPacing() {
    return pacing;
  }


  public void setPacing(PacingModel pacing) {
    this.pacing = pacing;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SponsoredProductsBudgetModel sponsoredProductsBudgetModel = (SponsoredProductsBudgetModel) o;
    return Objects.equals(this.amount, sponsoredProductsBudgetModel.amount) &&
        Objects.equals(this.cappings, sponsoredProductsBudgetModel.cappings) &&
        Objects.equals(this.pacing, sponsoredProductsBudgetModel.pacing);
  }

  private static <T> boolean equalsNullable(JsonNullable<T> a, JsonNullable<T> b) {
    return a == b || (a != null && b != null && a.isPresent() && b.isPresent() && Objects.deepEquals(a.get(), b.get()));
  }

  @Override
  public int hashCode() {
    return Objects.hash(amount, cappings, pacing);
  }

  private static <T> int hashCodeNullable(JsonNullable<T> a) {
    if (a == null) {
      return 1;
    }
    return a.isPresent() ? Arrays.deepHashCode(new Object[]{a.get()}) : 31;
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class SponsoredProductsBudgetModel {\n");
    sb.append("    amount: ").append(toIndentedString(amount)).append("\n");
    sb.append("    cappings: ").append(toIndentedString(cappings)).append("\n");
    sb.append("    pacing: ").append(toIndentedString(pacing)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("amount");
    openapiFields.add("cappings");
    openapiFields.add("pacing");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

 /**
  * Validates the JSON Object and throws an exception if issues found
  *
  * @param jsonObj JSON Object
  * @throws IOException if the JSON Object is invalid with respect to SponsoredProductsBudgetModel
  */
  public static void validateJsonObject(JsonObject jsonObj) throws IOException {
      if (jsonObj == null) {
        if (!SponsoredProductsBudgetModel.openapiRequiredFields.isEmpty()) { // has required fields but JSON object is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in SponsoredProductsBudgetModel is not found in the empty JSON string", SponsoredProductsBudgetModel.openapiRequiredFields.toString()));
        }
      }

      Set<Entry<String, JsonElement>> entries = jsonObj.entrySet();
      // check to see if the JSON string contains additional fields
      for (Entry<String, JsonElement> entry : entries) {
        if (!SponsoredProductsBudgetModel.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `SponsoredProductsBudgetModel` properties. JSON: %s", entry.getKey(), jsonObj.toString()));
        }
      }
      if (jsonObj.get("cappings") != null && !jsonObj.get("cappings").isJsonNull()) {
        JsonArray jsonArraycappings = jsonObj.getAsJsonArray("cappings");
        if (jsonArraycappings != null) {
          // ensure the json data is an array
          if (!jsonObj.get("cappings").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `cappings` to be an array in the JSON string but got `%s`", jsonObj.get("cappings").toString()));
          }

          // validate the optional field `cappings` (array)
          for (int i = 0; i < jsonArraycappings.size(); i++) {
            BudgetCappingModel.validateJsonObject(jsonArraycappings.get(i).getAsJsonObject());
          };
        }
      }
      // validate the optional field `pacing`
      if (jsonObj.get("pacing") != null && !jsonObj.get("pacing").isJsonNull()) {
        PacingModel.validateJsonObject(jsonObj.getAsJsonObject("pacing"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!SponsoredProductsBudgetModel.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'SponsoredProductsBudgetModel' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<SponsoredProductsBudgetModel> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(SponsoredProductsBudgetModel.class));

       return (TypeAdapter<T>) new TypeAdapter<SponsoredProductsBudgetModel>() {
           @Override
           public void write(JsonWriter out, SponsoredProductsBudgetModel value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public SponsoredProductsBudgetModel read(JsonReader in) throws IOException {
             JsonObject jsonObj = elementAdapter.read(in).getAsJsonObject();
             validateJsonObject(jsonObj);
             return thisAdapter.fromJsonTree(jsonObj);
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of SponsoredProductsBudgetModel given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of SponsoredProductsBudgetModel
  * @throws IOException if the JSON string is invalid with respect to SponsoredProductsBudgetModel
  */
  public static SponsoredProductsBudgetModel fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SponsoredProductsBudgetModel.class);
  }

 /**
  * Convert an instance of SponsoredProductsBudgetModel to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

